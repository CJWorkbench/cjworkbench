{
  "name": "Twitter",
  "id_name": "twitter",
  "category" : "Add data",
  "icon" : "twitter",
  "description" : "Load tweets and associated metadata for a specific user, or by searching globally for specific content.",
  "loads_data": true,
  "parameters": [
    {
      "name" : "Search for",
      "id_name" : "querytype",
      "type" : "menu",
      "menu_items" : "User tweets|Any tweet matching content",
      "default": "0"
    },
    {
      "name": "Username or content to match",
      "id_name" : "query",
      "type": "string",
      "default": ""
    },
    {
      "name": "Check Now",
      "id_name" : "version_select",
      "type": "custom"
    },
    {
      "name": "Add new data to current version",
      "id_name": "accumulate",
      "type": "checkbox",
      "default": "True"
    }
  ]
}
