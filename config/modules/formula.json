{
  "name": "Formula",
  "id_name": "formula",
  "category" : "Transform",
  "icon" : "api",
  "description": "Apply a formula between columns and generate its result in a new column.",
  "parameters": [
    {
      "name": "Type in formula, using column names",
      "id_name" : "formula",
      "type": "string",
      "default": ""
    },
    {
      "name": "Name the output column",
      "id_name" : "out_column",
      "type": "string",
      "default": "output"
    }
  ]
}
