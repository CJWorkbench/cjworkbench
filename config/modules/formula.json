{
  "name": "Formula",
  "id_name": "formula",
  "category" : "Transform",
  "icon" : "transform",
  "description": "Apply a formula between columns and store its result in a new column.",
  "parameters": [
    {
      "name": "Type in formula, using column names",
      "id_name" : "formula",
      "type": "string",
      "default": ""
    },
    {
      "name": "Name the output column",
      "id_name" : "out_column",
      "type": "string",
      "default": "output"
    }
  ]
}
