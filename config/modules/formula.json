{
  "name": "Formula",
  "id_name": "formula",
  "category" : "Analyse",
  "icon" : "sigma",
  "description": "Apply a formula between columns and store its result in a new column.",
  "help_url":"modules/analyse/formula",
  "parameters": [
    {
      "name": "Type in formula, using column names",
      "id_name" : "formula",
      "type": "string",
      "default": "",
      "placeholder": "e.g. column 1 * (column 2 + column 3)"
    },
    {
      "name": "Output column",
      "id_name" : "out_column",
      "type": "string",
      "default": "formula output"
    }
  ]
}
