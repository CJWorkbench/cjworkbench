{
  "name": "Add from URL",
  "id_name": "loadurl" ,
  "category" : "Add data",
  "icon" : "url",
  "description" : "Connect a dataset via its URL and keep it updated with its source.",
  "loads_data": true,
  "help_url":"modules/add-data/add-from-url",
  "parameters": [
    {
      "name": "Link to CSV, JSON or Excel file",
      "id_name" : "url",
      "type": "string",
      "default": "",
      "placeholder":"http://"
    },
    {
      "name": "Check",
      "id_name" : "version_select",
      "type": "custom"
    }
  ]
}
