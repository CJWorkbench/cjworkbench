{
  "name": "Add from URL",
  "id_name": "loadurl" ,
  "category" : "Add data",
  "icon" : "url",
  "description" : "Connect a dataset via its URL and keep it updated with its source.",
  "parameters": [
    {
      "name": "Link to CSV, JSON or xlsx",
      "id_name" : "url",
      "type": "string",
      "default": ""
    },
    {
      "name": "JSON path (optional)",
      "id_name" : "json_path",
      "type": "string",
      "default": ""
    },
    {
      "name": "Check Now",
      "id_name" : "version_select",
      "type": "custom"
    }
  ]
}
